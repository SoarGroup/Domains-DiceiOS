sp {dice*propose*exact
   (state <s> ^name dice
              ^my-turn true
              ^io.input-link.affordances.action <a>)
   (<a> ^name exact
        ^available true)
-->
   (<s> ^operator <op> +)
   (<op> ^name exact)
}

sp {apply*exact
   (state <s> ^operator <op>
              ^io.output-link <ol>)
   (<op> ^name exact)
-->
   (<ol> ^exact <exact>)
}
