sp {dice*propose*sleep
   (state <s> ^name dice
              ^my-turn false)
-->
   (<s> ^operator <op> + = 100)
   (<op> ^name sleep)
}

sp {apply*sleep
   (state <s> ^operator <op>
              ^io.output-link <ol>)
   (<op> ^name sleep)
-->
   (<ol> ^sleep <sleep>)
}
