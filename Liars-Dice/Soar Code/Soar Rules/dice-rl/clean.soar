sp {dice*propose*clean
   (state <s> ^name dice
              ^io.output-link.<action>.status)
-->
   (<s> ^operator <op> + >)
   (<op> ^name clean)
}

sp {apply*clean
   (state <s> ^operator <op>
              ^io.output-link <ol>)
   (<op> ^name clean)
   (<ol> ^<action-name> <action>)
   (<action> ^status)
-->
   (<ol> ^<action-name> <action> -)
}
